# Cloudflare Workers static assets — deploy from repo root with: npx wrangler deploy
name = "aisteth"
compatibility_date = "2024-05-24"

[assets]
directory = "frontend/dist"
not_found_handling = "single-page-application"
